%h2 固定費科目登録
= link_to "収入科目一覧に戻る", fixedcosts_path
=form_with model: @fixedcost do |f|
  %table
    %tr
      %th
        = f.label :name, "名称"
      %td
        = f.text_field :name
    %tr
      %th
        = f.label :description, "説明"
      %td
        = f.text_field :description
  = f.submit